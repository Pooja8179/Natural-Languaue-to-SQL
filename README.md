# Natural-Languaue-to-SQL
This project is a simple web app where you can ask questions in normal English, and the app automatically converts them into SQL queries using Google Gemini. The SQL query is then executed on a SQLite database, and the result is shown on the screen.
